// client/src/hooks/useAuth.js

// this is just a clean re-export of our context hook
// so components can import from hooks/ instead of context/
// keeps imports consistent across the project

export { useAuth } from "../context/AuthContext";

